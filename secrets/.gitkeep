Mount point for local secrets, exposed to the container at /run/secrets (read-only).

Put the Firebase service account JSON here as firebase-service-account.json, then
set FIREBASE_CREDENTIALS_FILE=/run/secrets/firebase-service-account.json in .env.

Everything in this directory except this file is gitignored.
